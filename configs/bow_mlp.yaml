seed: 42
max_length: 512
min_freq: 1

model:
  name: bow_mlp
  params:
    embedding_dim: 128
    hidden_dim: 128
    dropout: 0.3

training:
  batch_size: 16
  epochs: 5
  learning_rate: 0.001
  validation_split: 0.2
  threshold: 0.5

