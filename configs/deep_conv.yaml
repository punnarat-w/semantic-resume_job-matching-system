seed: 42
max_length: 512
min_freq: 1

model:
  name: deep_conv
  params:
    embedding_dim: 128
    num_filters: 256
    kernel_sizes: [2, 3, 4]
    dropout: 0.5

training:
  batch_size: 32
  epochs: 10
  learning_rate: 0.001
  validation_split: 0.2
  threshold: 0.5